# Copy this file to .env.local and add your real API key.
# .env.local is gitignored (*.local pattern) so your key stays safe.
#
# Without a key, the app uses mock AI generation (placeholder data).
VITE_OPENAI_API_KEY=your-key-here
